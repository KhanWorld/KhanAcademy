# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Comment
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 1 properties of this type in this kind.

    - property: author
      external_name: author
      # Type: User Stats: 32 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: content
      external_name: content
      # Type: Text Stats: 32 properties of this type in this kind.
      import_transform: db.Text

    - property: date
      external_name: date
      # Type: Date/Time Stats: 32 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: deleted
      external_name: deleted
      # Type: Boolean Stats: 32 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: targets
      external_name: targets
      # Type: Key Stats: 32 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string


- kind: DailyActivityLog
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 4174 properties of this type in this kind.

    - property: activity_summary
      external_name: activity_summary
      # Type: Blob Stats: 540012 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: date
      external_name: date
      # Type: Date/Time Stats: 540012 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: user
      external_name: user
      # Type: User Stats: 540012 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address


- kind: DiscussAnswer
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: author
      external_name: author
      # Type: User Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: content
      external_name: content
      # Type: Text Stats: 7 properties of this type in this kind.
      import_transform: db.Text

    - property: date
      external_name: date
      # Type: Date/Time Stats: 7 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: deleted
      external_name: deleted
      # Type: Boolean Stats: 7 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: targets
      external_name: targets
      # Type: Key Stats: 14 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string


- kind: DiscussQuestion
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: author
      external_name: author
      # Type: User Stats: 35 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: content
      external_name: content
      # Type: Text Stats: 35 properties of this type in this kind.
      import_transform: db.Text

    - property: date
      external_name: date
      # Type: Date/Time Stats: 35 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: deleted
      external_name: deleted
      # Type: Boolean Stats: 35 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: targets
      external_name: targets
      # Type: Key Stats: 35 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string


- kind: ExercisePlaylist
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 1 properties of this type in this kind.

    - property: exercise
      external_name: exercise
      # Type: Key Stats: 114 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: playlist
      external_name: playlist
      # Type: Key Stats: 114 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string


- kind: ExerciseVideo
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 5 properties of this type in this kind.

    - property: exercise
      external_name: exercise
      # Type: Key Stats: 242 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: video
      external_name: video
      # Type: Key Stats: 242 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string


- kind: Exercise
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 2 properties of this type in this kind.

    - property: author
      external_name: author
      # Type: User Stats: 50 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: covers
      external_name: covers
      # Type: String Stats: 53 properties of this type in this kind.

    - property: h_position
      external_name: h_position
      # Type: Integer Stats: 123 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: last_sanitized
      external_name: last_sanitized
      # Type: Date/Time Stats: 123 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: live
      external_name: live
      # Type: Boolean Stats: 123 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: name
      external_name: name
      # Type: String Stats: 123 properties of this type in this kind.

    - property: prerequisites
      external_name: prerequisites
      # Type: String Stats: 188 properties of this type in this kind.

    - property: seconds_per_fast_problem
      external_name: seconds_per_fast_problem
      # Type: Float Stats: 123 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: short_display_name
      external_name: short_display_name
      # Type: String Stats: 123 properties of this type in this kind.

    - property: summative
      external_name: summative
      # Type: Boolean Stats: 123 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: v_position
      external_name: v_position
      # Type: Integer Stats: 123 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: FeedbackNotification
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 74 properties of this type in this kind.

    - property: feedback
      external_name: feedback
      # Type: Key Stats: 8098 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: user
      external_name: user
      # Type: User Stats: 8098 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address


- kind: Feedback
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 300 properties of this type in this kind.

    - property: author
      external_name: author
      # Type: User Stats: 38291 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: content
      external_name: content
      # Type: Text Stats: 38291 properties of this type in this kind.
      import_transform: db.Text

    - property: date
      external_name: date
      # Type: Date/Time Stats: 38291 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: deleted
      external_name: deleted
      # Type: Boolean Stats: 38291 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: targets
      external_name: targets
      # Type: Key Stats: 53093 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: types
      external_name: types
      # Type: String Stats: 38291 properties of this type in this kind.


- kind: HourlyActivityLog
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 23 properties of this type in this kind.

    - property: activity_summary
      external_name: activity_summary
      # Type: Blob Stats: 2452 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: date
      external_name: date
      # Type: Date/Time Stats: 2452 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: user
      external_name: user
      # Type: User Stats: 2452 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address


- kind: Playlist
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 2 properties of this type in this kind.

    - property: description
      external_name: description
      # Type: Text Stats: 44 properties of this type in this kind.
      import_transform: db.Text

    - property: title
      external_name: title
      # Type: String Stats: 48 properties of this type in this kind.

    - property: url
      external_name: url
      # Type: String Stats: 48 properties of this type in this kind.

    - property: youtube_id
      external_name: youtube_id
      # Type: String Stats: 48 properties of this type in this kind.


- kind: ProblemLog
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 228783 properties of this type in this kind.

    - property: correct
      external_name: correct
      # Type: Boolean Stats: 29275564 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: earned_proficiency
      external_name: earned_proficiency
      # Type: Boolean Stats: 21297894 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: exercise
      external_name: exercise
      # Type: String Stats: 29275564 properties of this type in this kind.

    - property: exercise_non_summative
      external_name: exercise_non_summative
      # Type: String Stats: 851582 properties of this type in this kind.

    - property: hint_used
      external_name: hint_used
      # Type: Boolean Stats: 24946508 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: points_earned
      external_name: points_earned
      # Type: Integer Stats: 22852978 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: problem_number
      external_name: problem_number
      # Type: Integer Stats: 24946508 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: time_done
      external_name: time_done
      # Type: Date/Time Stats: 29275564 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: time_taken
      external_name: time_taken
      # Type: Integer Stats: 29275564 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: user
      external_name: user
      # Type: User Stats: 29275564 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address


- kind: QuestionAnswerSessionActionTypes
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: type
      external_name: type
      # Type: String Stats: 3 properties of this type in this kind.


- kind: QuestionAnswerSessionAction
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 1 properties of this type in this kind.

    - property: action
      external_name: action
      # Type: Integer Stats: 57 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    # Warning: This property is a duplicate, but with a different type.
    # TODO: Edit this transform so only one property with this name remains.
    - property: action
      external_name: action
      # Type: Key Stats: 23 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: session
      external_name: session
      # Type: Key Stats: 88 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: timestamp
      external_name: timestamp
      # Type: Date/Time Stats: 88 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')


- kind: QuestionAnswerSessionAttempt
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: answer_chosen_index
      external_name: answer_chosen_index
      # Type: Integer Stats: 88 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: answer_chosen_timestamp
      external_name: answer_chosen_timestamp
      # Type: Date/Time Stats: 88 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: session
      external_name: session
      # Type: Key Stats: 88 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: was_correct
      external_name: was_correct
      # Type: Boolean Stats: 88 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)


- kind: QuestionAnswerSession
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 1 properties of this type in this kind.

    - property: answerer
      external_name: answerer
      # Type: User Stats: 115 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: choice_0
      external_name: choice_0
      # Type: Integer Stats: 114 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: choice_1
      external_name: choice_1
      # Type: Integer Stats: 114 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: choice_2
      external_name: choice_2
      # Type: Integer Stats: 114 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: choice_3
      external_name: choice_3
      # Type: Integer Stats: 114 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: choice_4
      external_name: choice_4
      # Type: Integer Stats: 114 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: page_loaded_timestamp
      external_name: page_loaded_timestamp
      # Type: Date/Time Stats: 115 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: question
      external_name: question
      # Type: Key Stats: 115 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: total_attempts
      external_name: total_attempts
      # Type: Integer Stats: 115 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: QuestionAnswerer
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: answerer
      external_name: answerer
      # Type: User Stats: 52 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: difficulty_rating
      external_name: difficulty_rating
      # Type: Rating Stats: 52 properties of this type in this kind.
      import_transform: transform.none_if_empty(db.Rating)

    - property: difficulty_width
      external_name: difficulty_width
      # Type: Integer Stats: 52 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: importance_rating
      external_name: importance_rating
      # Type: Rating Stats: 52 properties of this type in this kind.
      import_transform: transform.none_if_empty(db.Rating)

    - property: importance_width
      external_name: importance_width
      # Type: Integer Stats: 52 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: quality_rating
      external_name: quality_rating
      # Type: Rating Stats: 52 properties of this type in this kind.
      import_transform: transform.none_if_empty(db.Rating)

    - property: quality_width
      external_name: quality_width
      # Type: Integer Stats: 52 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: question
      external_name: question
      # Type: Key Stats: 52 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string


- kind: Question
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: answer_text
      external_name: answer_text
      # Type: Text Stats: 34 properties of this type in this kind.
      import_transform: db.Text

    - property: author
      external_name: author
      # Type: User Stats: 34 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: correct_choice_text
      external_name: correct_choice_text
      # Type: String Stats: 9 properties of this type in this kind.

    # Warning: This property is a duplicate, but with a different type.
    # TODO: Edit this transform so only one property with this name remains.
    - property: correct_choice_text
      external_name: correct_choice_text
      # Type: Text Stats: 25 properties of this type in this kind.
      import_transform: db.Text

    - property: date_created
      external_name: date_created
      # Type: Date/Time Stats: 34 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: hint_text
      external_name: hint_text
      # Type: Text Stats: 25 properties of this type in this kind.
      import_transform: db.Text

    - property: incorrect_1
      external_name: incorrect_1
      # Type: String Stats: 9 properties of this type in this kind.

    # Warning: This property is a duplicate, but with a different type.
    # TODO: Edit this transform so only one property with this name remains.
    - property: incorrect_1
      external_name: incorrect_1
      # Type: Text Stats: 25 properties of this type in this kind.
      import_transform: db.Text

    - property: incorrect_2
      external_name: incorrect_2
      # Type: String Stats: 9 properties of this type in this kind.

    # Warning: This property is a duplicate, but with a different type.
    # TODO: Edit this transform so only one property with this name remains.
    - property: incorrect_2
      external_name: incorrect_2
      # Type: Text Stats: 25 properties of this type in this kind.
      import_transform: db.Text

    - property: incorrect_3
      external_name: incorrect_3
      # Type: String Stats: 9 properties of this type in this kind.

    # Warning: This property is a duplicate, but with a different type.
    # TODO: Edit this transform so only one property with this name remains.
    - property: incorrect_3
      external_name: incorrect_3
      # Type: Text Stats: 25 properties of this type in this kind.
      import_transform: db.Text

    - property: incorrect_4
      external_name: incorrect_4
      # Type: String Stats: 9 properties of this type in this kind.

    # Warning: This property is a duplicate, but with a different type.
    # TODO: Edit this transform so only one property with this name remains.
    - property: incorrect_4
      external_name: incorrect_4
      # Type: Text Stats: 25 properties of this type in this kind.
      import_transform: db.Text

    - property: incorrect_5
      external_name: incorrect_5
      # Type: String Stats: 9 properties of this type in this kind.

    # Warning: This property is a duplicate, but with a different type.
    # TODO: Edit this transform so only one property with this name remains.
    - property: incorrect_5
      external_name: incorrect_5
      # Type: Text Stats: 25 properties of this type in this kind.
      import_transform: db.Text

    - property: not_completed
      external_name: not_completed
      # Type: Boolean Stats: 34 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: published
      external_name: published
      # Type: Boolean Stats: 4 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: question_text
      external_name: question_text
      # Type: Text Stats: 34 properties of this type in this kind.
      import_transform: db.Text

    - property: subject
      external_name: subject
      # Type: Key Stats: 34 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string


- kind: Setting
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: value
      external_name: value
      # Type: String Stats: 4 properties of this type in this kind.


- kind: StemmedIndex
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 17 properties of this type in this kind.

    - property: parent_kind
      external_name: parent_kind
      # Type: String Stats: 2273 properties of this type in this kind.

    - property: phrases
      external_name: phrases
      # Type: String Stats: 38472 properties of this type in this kind.


- kind: Subject
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: author
      external_name: author
      # Type: User Stats: 64 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: index
      external_name: index
      # Type: Integer Stats: 63 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: name
      external_name: name
      # Type: String Stats: 64 properties of this type in this kind.

    - property: parent_subject
      external_name: parent_subject
      # Type: Key Stats: 63 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string


- kind: UserBadge
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 13911 properties of this type in this kind.

    - property: badge_name
      external_name: badge_name
      # Type: String Stats: 1792488 properties of this type in this kind.

    - property: date
      external_name: date
      # Type: Date/Time Stats: 1792488 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: points_earned
      external_name: points_earned
      # Type: Integer Stats: 1620868 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: target_context
      external_name: target_context
      # Type: Key Stats: 1375922 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: target_context_name
      external_name: target_context_name
      # Type: String Stats: 1375922 properties of this type in this kind.

    - property: user
      external_name: user
      # Type: User Stats: 1792488 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address


- kind: UserData
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 2976 properties of this type in this kind.

    - property: all_proficient_exercises
      external_name: all_proficient_exercises
      # Type: String Stats: 1469831 properties of this type in this kind.

    - property: badges
      external_name: badges
      # Type: String Stats: 1806944 properties of this type in this kind.

    - property: coaches
      external_name: coaches
      # Type: String Stats: 52347 properties of this type in this kind.

    - property: count_feedback_notification
      external_name: count_feedback_notification
      # Type: Integer Stats: 390468 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: expanded_all_exercises
      external_name: expanded_all_exercises
      # Type: Boolean Stats: 390468 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: joined
      external_name: joined
      # Type: Date/Time Stats: 390468 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: last_activity
      external_name: last_activity
      # Type: Date/Time Stats: 185222 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: last_daily_summary
      external_name: last_daily_summary
      # Type: Date/Time Stats: 389093 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: last_hourly_summary
      external_name: last_hourly_summary
      # Type: Date/Time Stats: 29574 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: last_login
      external_name: last_login
      # Type: Date/Time Stats: 343082 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: map_coords
      external_name: map_coords
      # Type: String Stats: 162536 properties of this type in this kind.

    - property: moderator
      external_name: moderator
      # Type: Boolean Stats: 390468 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: need_to_reassess
      external_name: need_to_reassess
      # Type: Boolean Stats: 343082 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: points
      external_name: points
      # Type: Integer Stats: 343080 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: proficient_exercises
      external_name: proficient_exercises
      # Type: String Stats: 1370810 properties of this type in this kind.

    - property: suggested_exercises
      external_name: suggested_exercises
      # Type: String Stats: 887553 properties of this type in this kind.

    - property: total_seconds_watched
      external_name: total_seconds_watched
      # Type: Integer Stats: 390468 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: user
      external_name: user
      # Type: User Stats: 343082 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: videos_completed
      external_name: videos_completed
      # Type: Integer Stats: 390468 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: UserExercise
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 16166 properties of this type in this kind.

    - property: exercise
      external_name: exercise
      # Type: String Stats: 2076522 properties of this type in this kind.

    - property: exercise_model
      external_name: exercise_model
      # Type: Key Stats: 2076466 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: first_done
      external_name: first_done
      # Type: Date/Time Stats: 2076522 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: last_done
      external_name: last_done
      # Type: Date/Time Stats: 2076511 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: last_review
      external_name: last_review
      # Type: Date/Time Stats: 2076522 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: longest_streak
      external_name: longest_streak
      # Type: Integer Stats: 2076511 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: proficient_date
      external_name: proficient_date
      # Type: Date/Time Stats: 1173871 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: review_interval_secs
      external_name: review_interval_secs
      # Type: Integer Stats: 2076522 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: seconds_per_fast_problem
      external_name: seconds_per_fast_problem
      # Type: Float Stats: 2076522 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: streak
      external_name: streak
      # Type: Integer Stats: 2076522 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: summative
      external_name: summative
      # Type: Boolean Stats: 2076522 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: total_done
      external_name: total_done
      # Type: Integer Stats: 2076511 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: user
      external_name: user
      # Type: User Stats: 2076522 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address


- kind: UserPlaylist
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 2638 properties of this type in this kind.

    - property: last_watched
      external_name: last_watched
      # Type: Date/Time Stats: 338887 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: playlist
      external_name: playlist
      # Type: Key Stats: 338887 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: seconds_watched
      external_name: seconds_watched
      # Type: Integer Stats: 338887 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: title
      external_name: title
      # Type: String Stats: 338283 properties of this type in this kind.

    - property: user
      external_name: user
      # Type: User Stats: 338887 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address


- kind: UserVideo
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 9160 properties of this type in this kind.

    - property: completed
      external_name: completed
      # Type: Boolean Stats: 995634 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: duration
      external_name: duration
      # Type: Integer Stats: 1105983 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: last_second_watched
      external_name: last_second_watched
      # Type: Integer Stats: 1105983 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: last_watched
      external_name: last_watched
      # Type: Date/Time Stats: 1181060 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: percent_watched
      external_name: percent_watched
      # Type: Float Stats: 79970 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: seconds_watched
      external_name: seconds_watched
      # Type: Integer Stats: 1181060 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: user
      external_name: user
      # Type: User Stats: 1181060 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: video
      external_name: video
      # Type: Key Stats: 1181060 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string


- kind: VideoLog
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 117720 properties of this type in this kind.

    - property: playlist_titles
      external_name: playlist_titles
      # Type: String Stats: 12995961 properties of this type in this kind.

    - property: points_earned
      external_name: points_earned
      # Type: Integer Stats: 13865236 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: seconds_watched
      external_name: seconds_watched
      # Type: Integer Stats: 15051335 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: time_watched
      external_name: time_watched
      # Type: Date/Time Stats: 15051335 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: user
      external_name: user
      # Type: User Stats: 15051335 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: video
      external_name: video
      # Type: Key Stats: 15051335 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: video_title
      external_name: video_title
      # Type: String Stats: 15051261 properties of this type in this kind.


- kind: VideoPlaylist
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 12 properties of this type in this kind.

    - property: last_live_association_generation
      external_name: last_live_association_generation
      # Type: Integer Stats: 2351 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: live_association
      external_name: live_association
      # Type: Boolean Stats: 2351 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: playlist
      external_name: playlist
      # Type: Key Stats: 2351 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: video
      external_name: video
      # Type: Key Stats: 2351 properties of this type in this kind.
      import_transform: transform.create_foreign_key('TODO: fill in Kind name')
      export_transform: transform.key_id_or_name_as_string

    - property: video_position
      external_name: video_position
      # Type: Integer Stats: 2351 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: Video
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 24 properties of this type in this kind.

    - property: date_added
      external_name: date_added
      # Type: Date/Time Stats: 2203 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: description
      external_name: description
      # Type: Text Stats: 2195 properties of this type in this kind.
      import_transform: db.Text

    - property: duration
      external_name: duration
      # Type: Integer Stats: 2205 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: keywords
      external_name: keywords
      # Type: String Stats: 2225 properties of this type in this kind.

    - property: playlists
      external_name: playlists
      # Type: String Stats: 2458 properties of this type in this kind.

    - property: readable_id
      external_name: readable_id
      # Type: String Stats: 2225 properties of this type in this kind.

    - property: title
      external_name: title
      # Type: String Stats: 2225 properties of this type in this kind.

    - property: url
      external_name: url
      # Type: String Stats: 2225 properties of this type in this kind.

    - property: views
      external_name: views
      # Type: Integer Stats: 2203 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: youtube_id
      external_name: youtube_id
      # Type: String Stats: 2225 properties of this type in this kind.


- kind: YouTubeSyncStepLog
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 1 properties of this type in this kind.

    - property: dt
      external_name: dt
      # Type: Date/Time Stats: 47 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: generation
      external_name: generation
      # Type: Integer Stats: 47 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: step
      external_name: step
      # Type: Integer Stats: 47 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: _AE_MR_MapreduceControl
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: command
      external_name: command
      # Type: Text Stats: 8 properties of this type in this kind.
      import_transform: db.Text


- kind: _AE_MR_MapreduceState
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 2 properties of this type in this kind.

    - property: aborted_shards
      external_name: aborted_shards
      # Type: Integer Stats: 255 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: active
      external_name: active
      # Type: Boolean Stats: 255 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: active_shards
      external_name: active_shards
      # Type: Integer Stats: 255 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: chart_url
      external_name: chart_url
      # Type: Text Stats: 255 properties of this type in this kind.
      import_transform: db.Text

    - property: counters_map
      external_name: counters_map
      # Type: Text Stats: 255 properties of this type in this kind.
      import_transform: db.Text

    - property: failed_shards
      external_name: failed_shards
      # Type: Integer Stats: 255 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: last_poll_time
      external_name: last_poll_time
      # Type: Date/Time Stats: 255 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: mapreduce_spec
      external_name: mapreduce_spec
      # Type: Text Stats: 255 properties of this type in this kind.
      import_transform: db.Text

    - property: result_status
      external_name: result_status
      # Type: String Stats: 255 properties of this type in this kind.

    - property: sparkline_url
      external_name: sparkline_url
      # Type: Text Stats: 255 properties of this type in this kind.
      import_transform: db.Text

    - property: start_time
      external_name: start_time
      # Type: Date/Time Stats: 255 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')


- kind: _AE_MR_ShardState
  connector: # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: __scatter__
      external_name: __scatter__
      # Type: ShortBlob Stats: 80 properties of this type in this kind.

    - property: active
      external_name: active
      # Type: Boolean Stats: 9897 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: counters_map
      external_name: counters_map
      # Type: Text Stats: 9897 properties of this type in this kind.
      import_transform: db.Text

    - property: last_work_item
      external_name: last_work_item
      # Type: Text Stats: 9897 properties of this type in this kind.
      import_transform: db.Text

    - property: mapreduce_id
      external_name: mapreduce_id
      # Type: String Stats: 9897 properties of this type in this kind.

    - property: result_status
      external_name: result_status
      # Type: String Stats: 9897 properties of this type in this kind.

    - property: shard_description
      external_name: shard_description
      # Type: Text Stats: 9897 properties of this type in this kind.
      import_transform: db.Text

    - property: update_time
      external_name: update_time
      # Type: Date/Time Stats: 9897 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

